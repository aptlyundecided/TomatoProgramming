# Very common sense
# absolute value disregards negative value and reflects
# only a magnitude as a positive int

raw = -100
absolute = abs(raw)

print('raw: ' + str(raw))
print('absolute: ' + str(absolute))
